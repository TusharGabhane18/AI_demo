# AI Agent Demo Playwright Project

This folder contains a Playwright test project scaffolded for the AI_Agent_Demo workspace.

## Commands

- `npm install` - install Playwright and dependencies
- `npm test` - run Playwright tests
- `npm run test:headed` - run tests in headed mode
- `npm run test:trace` - run tests with trace enabled